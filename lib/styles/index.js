const React = require('react-native')
const {StyleSheet} = React
const header = StyleSheet.create(require('./header'))
const utils = StyleSheet.create(require('./utils'))

module.exports = {header, utils}
